# Database
DB_HOST=localhost
DB_USER=securevpnuser
DB_PASSWORD=$(openssl rand -hex 32)
DB_NAME=securevpn
DB_SSLMODE=verify-full

# JWT
JWT_SECRET=$(openssl rand -hex 64)

# TLS
TLS_CERT=/path/to/fullchain.pem
TLS_KEY=/path/to/privkey.pem

# Rate Limiting
RATE_LIMIT=100
RATE_WINDOW=1m